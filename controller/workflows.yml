---
controller_workflows:
  - name: Workflow_with_approval
    allow_simultaneous: false
    ask_variables_on_launch: false
    state: present
    ask_inventory_on_launch: false
    ask_scm_branch_on_launch: false
    ask_limit_on_launch: false
    organization: customer-x
    simplified_workflow_nodes:
      - all_parents_must_converge: false
        identifier: First_Job
        credentials: []
        unified_job_template: Check if web frontend is alive
        success_nodes:
          - approval_task
        failure_nodes:
          - Second_Job
      - all_parents_must_converge: false
        identifier: Second_Job
        credentials: []
        unified_job_template: Other check if web frontend
      - identifier: approval_task
        approval_node:
            name: approval_task
            description: For testing approval process
